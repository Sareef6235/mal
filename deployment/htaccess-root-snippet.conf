# Place this in your public_html/.htaccess (domain root), not inside /qwe3.
# 1) Redirect wrong path to the correct dashboard path.
Redirect 301 /wqa/dashboard.php /qwe3/dashboard.php

# 2) OPTIONAL: only needed if you intentionally embed in iframe.
#    If you are using direct navigation (recommended), you can skip this.
#<IfModule mod_headers.c>
#    Header always unset X-Frame-Options
#</IfModule>
